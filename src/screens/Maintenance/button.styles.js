import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    primaryButton: {
        flex: 1,
        marginHorizontal: 15,
        paddingVertical: 30,
        marginTop: 20
      }
});